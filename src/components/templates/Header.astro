---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<header class="flex justify-center items-center h-12 w-full bg-main">
  <h1 class="text-3xl font-bold text-white">{title}</h1>
  <!-- TODO メニューを追加予定 -->
</header>
